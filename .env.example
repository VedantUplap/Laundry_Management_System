# =============================================================================
# LSMS — Example Configuration File (.env.example)
# Copy to .env and adjust credentials as needed
# =============================================================================

# Database Configuration (MySQL 8.0+)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lsms

# Java Web Server Configuration
PORT=3001
JWT_SECRET=lsms_super_secret_jwt_key_change_in_production_2024
JWT_EXPIRES_IN_HOURS=24
